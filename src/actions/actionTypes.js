// These are the names of the functions the Reducer (Redux fxn) will use to figure out which function to run depending on the Action type (data type) received

export const DELETE_USER = "DELETE_USER";          // It's convention to use all caps. Creating this variable helps reduce typos later

export const ADD_USER = "ADD_USER";                // It's convention to use all caps. Creating this variable helps reduce typos later