// These are the names of the functions the Reducer (Redux fxn) will use to figure out which function to run depending on the Action type (data type) received

export const DELETE_USER = "DELETE_USER";          // It's convention to use all caps. Creating this variable name that's equal to a string value helps you more easily ID typos when referencing this elsewhere in your app. 

export const ADD_USER = "ADD_USER";                // It's convention to use all caps. Creating this variable name that's equal to a string value helps you more easily ID typos when referencing this elsewhere in your app. 